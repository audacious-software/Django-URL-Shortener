from django.apps import AppConfig

class UrlShortenerConfig(AppConfig):
    name = 'url_shortener'
    verbose_name = 'URL Shortener & Tracker'
    default_auto_field = 'django.db.models.BigAutoField'
